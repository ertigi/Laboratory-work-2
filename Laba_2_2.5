#include <stdio.h>
#include <locale.h>
//Посчитать количество знаков препинания в строке

#define N 100

main()
{
	setlocale(LC_ALL, "RUS");

	int arr[N];
	int arr2[N];
	int p, c, i = 0;
	c = mass(&arr[0]);
// копирование массива
	while (i <= c) {
		if (i == c) {
			arr2[i] = '\n';
			++i;
			continue;
		}
		arr2[i] = arr[i];
		++i;
	}

	for(;;) {
		p = prov(&arr2[0], c);
		if (p == 0)
			delete_mass(&arr2[0], c);
		else {
			printf("-----\n");
			break;
		}
	}
	print_mass(&arr2[0], c);

}
// поэлементная запись в массив
int mass(int a[])
{
	int c, i = 0;
	int *arr;
	arr = a;
	while ((c = getchar()) != EOF) {
		arr[i] = c;
		++i;
	}
	return(i);
}
// поэлементный вывод 
int print_mass(int *a, int c)
{
	int i = 0;
	int *arr;
	arr = a;
	while (i < c) {
		putchar(arr[i]);
		++i;
	}
}
// проверка скобок 
int prov(int *a, int c)
{
	int i, i1, i2, s = 0;

	for (i = 0; i < c; ++i) {
		if (a[i] == '(') {
			i1 = i;
			++s;
		}
		if (a[i] == ')') {
			i2 = i;
			--s;
		}
		if (s < 0)
			break;
	}
	return(s);
}
// удаление элементов 
int delete_mass(int *a, int c)
{
	int i;

	while (i < c) {

	}
}
