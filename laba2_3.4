#include <stdio.h>
#include <stdlib.h>


#define N 7
#define M 3

int Input_array(int arr[N][M])
{
	for (int i = 0; i < N; i++)
	{
		for (int j = 0; j < M; j++)
		{
			printf("arr[%d][%d] = ", i, j);
			scanf_s("%d", &arr[i][j]);
		}
	}
	printf("\n");
	return 0;
}

int Output_array(int *arr)
{
	int q = 1, c = 0;

	for (int i = 0; i < N; i++)
	{
		for (int j = 0; j < M; j++)
		{
			if (i % 2 == 0)
			{
				printf("%3.0d", arr[c]);
			}
			else
			{
				printf("%3.0d", arr[((i+1) * M) - (j+1)]);
			}
			c++;
		}
		printf("\n");
	}
	return 0;
}

int shift(int *arr)
{
	int w = 0;
	for (int i = 0; i < (N*M); i++)
	{
		for (int j = (N*M) - 1; j > i; j--)
		{
			if (arr[j - 1] > arr[j])
			{
				int c = arr[j - 1];
				arr[j - 1] = arr[j];
				arr[j] = c;
			}
		}
	}
}

int main()
{
	int i, j, c = 0;
	int mas[N][M];
	for (i = 0; i < N; i++)
		for (j = 0; j < M; j++)
			mas[i][j] = 0;

	Input_array(mas);

	int mas2[(N*M)];
	for (i = 0; i < N; i++)
	{
		for (j = 0; j < M; j++)
		{
			mas2[c] = mas[i][j];
			c++;
		}
	}

	shift(mas2);

	Output_array(mas2);

	exit(1);
}
